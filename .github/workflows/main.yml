name: Fast_Diamond_💎
on:
  workflow_dispatch:
  schedule:
    - cron: '*/1 * * * *' # تشغيل تلقائي كل دقيقة واحدة
jobs:
  fast_run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - run: pip install requests beautifulsoup4 pyTelegramBotAPI
      - run: python scraper.py
